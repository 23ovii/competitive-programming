#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    if (n < 5) {
        cout << "corigent";
    } else {
        cout << "promovat";
    }
    return 0;
}